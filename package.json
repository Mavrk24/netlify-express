{
    "name": "my-app",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
      
    "cors": "^2.8.5",
    "express": "^4.17.1"
        
    },
    "scripts": {
      "start-program": "node server.js && react-scripts start && yarn start-api",
      "start": "react-scripts start",
      "start-api": "cd src && set FLASK_APP=api.py && py -m flask run --no-debugger",
      "build": "npm install",
      "test": "react-scripts test",
      "eject": "react-scripts eject"
    },
    "eslintConfig": {
      "extends": [
        "react-app",
        "react-app/jest"
      ]
    },
    "browserslist": {
      "production": [
        ">0.2%",
        "not dead",
        "not op_mini all"
      ],
      "development": [
        "last 1 chrome version",
        "last 1 firefox version",
        "last 1 safari version"
      ]
    },
    "engines":{
        "node": "13.14.0"
    }
    ,
    "devDependencies": {
        "cors": "^2.8.5",
      "express": "^4.17.1"
      }
  }
  
